// const bcrypt = require('bcrypt');

// const comparePasswordMiddleware = async function (password) {
//   try {
//     return await bcrypt.compare(password, this.password);
//   } catch (error) {
//     throw error;
//   }
// };

// module.exports = comparePasswordMiddleware;
